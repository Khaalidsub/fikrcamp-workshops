// show a type data and that could be used within a function
// types are used for typing objects much more easier
type ProductData = {
  id: string;
  name: string;
};

function createProduct(product: Product) {
  return product;
}
